PORT=8080
CORS_ORIGIN=*
UPLOAD_PATH=images

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=socialogy
# Schema is owned by TypeORM migrations. DB_SYNC is ignored (always off).
DB_SYNC=0

JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_IN=1h

# Backup (host tools against local Docker Postgres):
#   docker compose exec postgres pg_dump -U postgres socialogy > backup.sql
# Restore:
#   docker compose exec -T postgres psql -U postgres socialogy < backup.sql

# Phase 6 — Mongo → PostgreSQL data migration (npm run migrate:mongo)
# MONGODB_URI=mongodb://localhost:27017/socialogy
# MIGRATE_IMAGES_SRC=../social-network-app-backend/images
# MIGRATE_IMAGES_DEST=images
